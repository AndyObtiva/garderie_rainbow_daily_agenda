- Fix display of table of potty times as x instead of true for checkbox fields
- Fix display of logo on older Mac Mojave having the wrong offset (looking like it repeats)
- Make changing start time affect end time if it surpasses it and vice versa
- Make c_date_time widgets open up when clicking inside the content not just the icon
- Break steps down into a wizard to avoid the need for scrolling
- Write an architecture/design document
